import { author,articles } from '../content/articles'; import { ArticleCard } from '../components/ArticleCard'; import { SEO } from '../components/SEO'; import { SITE } from '../lib/config';
export default function Author(){return <><SEO title={author.name} description={author.bio} path="/penulis/nadia-pratama" jsonLd={{'@context':'https://schema.org','@type':'Person',name:author.name,jobTitle:author.role,url:`${SITE.url}/penulis/nadia-pratama`}}/><header className="page-hero"><img src={author.avatar} alt={`Foto ${author.name}`} className="mx-auto h-28 w-28 rounded-full object-cover" width="112" height="112"/><h1 className="mt-5 font-display text-4xl font-bold">{author.name}</h1><p className="mt-1 font-semibold text-brand-600">{author.role}</p><p className="mx-auto mt-4 max-w-xl text-slate-500">{author.bio}</p></header><div className="container-shell py-12"><h2 className="font-display text-3xl font-bold">Tulisan terbaru</h2><div className="mt-7 grid gap-6 md:grid-cols-3">{articles.slice(0,3).map(a=><ArticleCard key={a.slug} article={a}/>)}</div></div></>}
