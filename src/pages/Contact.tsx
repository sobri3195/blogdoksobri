import { FormEvent,useState } from 'react'; import { SEO } from '../components/SEO';
export default function Contact(){const [status,setStatus]=useState('');function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();const d=new FormData(e.currentTarget);if(String(d.get('name')).trim().length<2||!/^\S+@\S+\.\S+$/.test(String(d.get('email')))||String(d.get('message')).trim().length<10){setStatus('Periksa kembali nama, email, dan pesan (minimal 10 karakter).');return}setStatus('Pesan tervalidasi. Hubungkan formulir ini ke layanan backend sebelum publikasi.')}return <><SEO title="Kontak" description="Hubungi tim editorial Ruang Tumbuh." path="/kontak"/><header className="page-hero"><h1 className="font-display text-4xl font-bold">Mari berbicara</h1><p className="mt-3 text-slate-500">Pertanyaan, koreksi, atau ide artikel—kami siap mendengarkan.</p></header><div className="container-shell grid gap-10 py-12 md:grid-cols-[.7fr_1.3fr]"><div><h2 className="font-display text-2xl font-bold">Kontak editorial</h2><p className="mt-3 text-sm leading-6 text-slate-500">Formulir demo ini belum mengirim data. Sambungkan ke Formspree, Resend, atau server Anda sebelum website dipublikasikan.</p><p className="mt-5 text-sm"><strong>Waktu respons</strong><br/>2–3 hari kerja</p></div><form onSubmit={submit} noValidate className="rounded-2xl border bg-white p-6 dark:border-slate-700 dark:bg-slate-900"><div className="mb-5"><label className="field-label" htmlFor="name">Nama</label><input className="form-field" id="name" name="name" required/></div><div className="mb-5"><label className="field-label" htmlFor="email">Email</label><input className="form-field" id="email" name="email" type="email" required/></div><div className="mb-5"><label className="field-label" htmlFor="message">Pesan</label><textarea className="form-field min-h-36" id="message" name="message" required/></div><button className="btn-primary">Kirim pesan</button>{status&&<p role="alert" className="mt-4 text-sm">{status}</p>}</form></div></>}
