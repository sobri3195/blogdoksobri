import { useParams,Link } from 'react-router-dom'; import { articles } from '../content/articles'; import { ArticleCard } from '../components/ArticleCard'; import { SEO } from '../components/SEO';
export default function Category(){const {slug=''}=useParams();const category=articles.find(a=>a.category.toLowerCase()===slug)?.category||slug;const list=articles.filter(a=>a.category.toLowerCase()===slug);return <><SEO title={`Kategori ${category}`} description={`Artikel terbaik dalam kategori ${category}.`} path={`/kategori/${slug}`}/><header className="page-hero"><span className="eyebrow">Kategori</span><h1 className="mt-3 font-display text-4xl font-bold capitalize">{category}</h1><p className="mt-3 text-slate-500">{list.length} artikel untuk Anda jelajahi.</p></header><div className="container-shell py-12">{list.length?<div className="grid gap-7 md:grid-cols-2 lg:grid-cols-3">{list.map(a=><ArticleCard key={a.slug} article={a}/>)}</div>:<div className="text-center"><p>Belum ada artikel di kategori ini.</p><Link to="/blog" className="btn-primary mt-5">Lihat semua artikel</Link></div>}</div></>}
