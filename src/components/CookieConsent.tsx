import { useState } from 'react';
export function CookieConsent(){const [open,setOpen]=useState(!localStorage.getItem('cookie-consent'));const [manage,setManage]=useState(false);const [analytics,setAnalytics]=useState(false);const [ads,setAds]=useState(false);if(!open)return null;const save=(value:'accepted'|'rejected'|'custom')=>{localStorage.setItem('cookie-consent',value);localStorage.setItem('cookie-analytics',String(value==='accepted'||analytics));localStorage.setItem('cookie-advertising',String(value==='accepted'||ads));window.dispatchEvent(new Event('consent-updated'));setOpen(false)};return <div className="fixed inset-x-4 bottom-4 z-[80] mx-auto max-w-3xl rounded-2xl border border-slate-200 bg-white p-5 shadow-2xl dark:border-slate-700 dark:bg-slate-900" role="dialog" aria-label="Preferensi cookie"><h2 className="font-bold text-lg">Privasi Anda, pilihan Anda</h2><p className="mt-1 text-sm text-slate-600 dark:text-slate-300">Kami menggunakan penyimpanan yang diperlukan dan, dengan izin Anda, analitik serta iklan. Pengelola situs wajib menyesuaikannya dengan hukum wilayah operasional.</p>{manage&&<div className="my-4 grid gap-2 text-sm"><label className="flex justify-between">Necessary <input type="checkbox" checked disabled/></label><label className="flex justify-between">Analytics <input type="checkbox" checked={analytics} onChange={e=>setAnalytics(e.target.checked)}/></label><label className="flex justify-between">Advertising <input type="checkbox" checked={ads} onChange={e=>setAds(e.target.checked)}/></label></div>}<div className="mt-4 flex flex-wrap gap-2"><button className="btn-primary" onClick={()=>save('accepted')}>Terima</button><button className="btn-secondary" onClick={()=>save('rejected')}>Tolak</button><button className="btn-secondary" onClick={()=>manage?save('custom'):setManage(true)}>{manage?'Simpan preferensi':'Kelola preferensi'}</button></div></div>}
