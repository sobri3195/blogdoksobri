import { useEffect } from 'react'; import { SITE } from '../lib/config';
interface Props{title:string;description:string;path?:string;image?:string;noindex?:boolean;jsonLd?:Record<string,unknown>|Record<string,unknown>[]}
const upsert=(selector:string,key:string,keyValue:string,value:string)=>{let el=document.head.querySelector<HTMLMetaElement>(selector);if(!el){el=document.createElement('meta');el.setAttribute(key,keyValue);document.head.appendChild(el)}el.setAttribute('content',value)};
export function SEO({title,description,path='/',image,noindex,jsonLd}:Props){useEffect(()=>{document.title=`${title} — ${SITE.name}`;upsert('meta[name="description"]','name','description',description);upsert('meta[property="og:title"]','property','og:title',title);upsert('meta[property="og:description"]','property','og:description',description);upsert('meta[property="og:type"]','property','og:type','website');upsert('meta[name="twitter:card"]','name','twitter:card','summary_large_image');if(image)upsert('meta[property="og:image"]','property','og:image',image);upsert('meta[name="robots"]','name','robots',noindex?'noindex, follow':'index, follow');let canonical=document.head.querySelector<HTMLLinkElement>('link[rel="canonical"]');if(!canonical){canonical=document.createElement('link');canonical.rel='canonical';document.head.appendChild(canonical)}canonical.href=`${SITE.url}${path}`;document.getElementById('page-jsonld')?.remove();if(jsonLd){const s=document.createElement('script');s.id='page-jsonld';s.type='application/ld+json';s.textContent=JSON.stringify(jsonLd);document.head.appendChild(s)}},[title,description,path,image,noindex,jsonLd]);return null}
