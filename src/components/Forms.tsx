import { FormEvent, useState } from 'react'; import { ArrowRight } from 'lucide-react';
export function Newsletter(){const [status,setStatus]=useState('');function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();const data=new FormData(e.currentTarget);const email=String(data.get('email'));setStatus(/^\S+@\S+\.\S+$/.test(email)?'Terima kasih! Silakan periksa email Anda.':'Masukkan alamat email yang valid.')}return <section className="newsletter"><div><span className="eyebrow text-blue-100">Surat mingguan</span><h2 className="mt-2 text-2xl md:text-3xl font-display font-bold">Satu ide baik, setiap Sabtu.</h2><p className="mt-2 text-blue-100">Ringkas, praktis, tanpa spam. Berhenti kapan saja.</p></div><form onSubmit={submit} noValidate className="mt-6 lg:mt-0 lg:min-w-[420px]"><label htmlFor="newsletter-email" className="sr-only">Alamat email</label><div className="flex rounded-xl bg-white p-1.5"><input id="newsletter-email" name="email" type="email" placeholder="email@contoh.com" className="min-w-0 flex-1 bg-transparent px-3 text-slate-900 outline-none"/><button className="btn-primary" type="submit">Berlangganan <ArrowRight size={16}/></button></div>{status&&<p role="status" className="mt-2 text-sm text-blue-50">{status}</p>}</form></section>}
