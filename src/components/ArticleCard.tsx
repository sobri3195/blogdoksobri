import { Link } from 'react-router-dom'; import { ArrowUpRight, Clock3 } from 'lucide-react'; import type { Article } from '../types';
export function ArticleCard({article,large=false}: {article:Article;large?:boolean}){return <article className={`group card ${large?'md:grid md:grid-cols-[1.15fr_.85fr]':''}`}><Link to={`/blog/${article.slug}`} className="image-wrap" aria-label={`Baca ${article.title}`}><img src={article.image} alt={article.imageAlt} width="800" height="520" loading={large?'eager':'lazy'}/></Link><div className="p-5 md:p-6"><div className="flex items-center gap-3 text-xs font-bold uppercase tracking-wider text-brand-600"><span>{article.category}</span><span className="normal-case font-medium text-slate-400 flex items-center gap-1"><Clock3 size={13}/>{article.readingTime} mnt</span></div><h3 className={`${large?'text-2xl md:text-3xl':'text-xl'} mt-3 font-display font-bold leading-tight text-slate-900 dark:text-white`}><Link to={`/blog/${article.slug}`} className="hover:text-brand-600">{article.title}</Link></h3><p className="mt-3 text-sm leading-6 text-slate-600 dark:text-slate-300">{article.excerpt}</p><Link to={`/blog/${article.slug}`} className="mt-5 inline-flex items-center gap-1 text-sm font-bold text-brand-600">Baca artikel <ArrowUpRight size={15}/></Link></div></article>}
